* Concert Archives export and visualization
* Create a photo gallery
* refactor CSS to cut out tailwind
* homepage is basically a /now page. Implement a little kitty-like sparkline for action per day
  over X period of time?
* wandrer
* post to instagram and other places?
* about me / about site
* discord integration?
* allow anything to be pin-nable as a highlight (may want comments, so it'd need to be a separate
  table)


Code: Write in MDX -> schema-dts validates data -> astro-og-canvas makes images.
Build: astro build runs -> astro-link-check validates internal paths.
Audit: GitHub Action runs Lighthouse CI & Axe-core -> Fails if quality drops.
Deploy: Push to host -> Script triggers IndexNow & WebSub.
Monitor: Check Clarity for user behavior & Ahrefs for backlink health.
