{
  "name": "web",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "clean": "rm -rf dist .astro src/data/*.json",
    "fetch": "node scripts/flickr-fetch.mjs && node scripts/trakt-fetch.mjs && node scripts/strava-fetch.mjs && node scripts/music-fetch.mjs",
    "test": "vitest run tests/",
    "build": "npm run clean && npm run test && npm run fetch && astro build"
  },
  "devDependencies": { "vitest": "^4.0.16", "astro": "^5.13.9" }
}
